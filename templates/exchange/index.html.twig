{% extends 'base.html.twig' %}

{% block title %}Hello ExchangeController!{% endblock %}


{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

   Wprowadź datę, która cie interesuje! Następnie wciśnij "pokaż"

    <form action="{{ path('app_getExchangeData') }}" type="post">
        <input type="date" name="date_from" id="date_from">
        <input type="submit" value="Pokaż">
        {#            <input type="button" data-href="{{ path('get_data', {'date_from': }) }}">#}
    </form>
    {% block table %}
    {% endblock %}
</div>
<script>
    document.getElementById('date_from').max = new Date(new Date().getTime() - new Date().getTimezoneOffset() * 60000).toISOString().split("T")[0];
</script>
{% endblock %}
